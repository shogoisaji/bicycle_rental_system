name: bicycle_rental_system
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router:
  google_sign_in:
  google_fonts:
  flutter_riverpod:
  firebase_auth:
  cloud_firestore:
  path_provider: any
  riverpod_annotation: ^2.1.1
  image_picker:
  firebase_core: ^2.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  riverpod_generator:

flutter:
  uses-material-design: true
  # assets:
    # - images/